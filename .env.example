# Atlas API host the twin chat talks to.
# Unset  -> mock mode (no backend needed; the demo still runs)
# Set    -> live mode against that Atlas instance
#
# Copy to .env.local and point at staging:
#   VITE_ATLAS_API_URL=https://<atlas-staging-host>
VITE_ATLAS_API_URL=
